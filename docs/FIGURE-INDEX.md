# SQL-SI Figure Index

## Fig-001 — SQL-SI Layered Architecture

Shows SQL-SI as a five-layer Structural Intelligence backend:

Raw Data → SQL Relational Layer → Trajectory / Graph Layer → CCC Engine → PDS / Policy Layer.

## Fig-002 — SQL to SQL-SI Flow

Shows the transformation from traditional SQL recomputation into reusable structural objects:

Rows → Trajectory → CCC → MetricPoint → Structural Store → Reuse.

## Fig-003 — SQL vs SQL-SI Comparative Runtime

Contrasts query-time recomputation in traditional SQL with persistent reusable CCC / trajectory / metric objects in SQL-SI.